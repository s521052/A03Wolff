
QUnit.test("Practice Test", function (assert) {
    assert.ok(1 <= "3", "1<3 - the first agrument is 'truthy', so we pass!");
});

QUnit.test( "A02 QUnit Test", function( assert ) {
    assert.ok( 1 == "1", "Passed!" );
    assert.ok( 1 == "1", "Passed!" );
    assert.ok( 1 == "1", "Passed!" );
    assert.ok( 1 == "1", "Passed!" );
});
